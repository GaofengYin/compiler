---
- name: install a package
  package:
   name: vsftpd
   state: present
- name: check service
  service:
   name: vsftpd
   state: started
   enabled: yes
- name: check a file
  file:
   mode: 644
   owner: root
   group: root
   state: present
   path: /etc/vsftpd/vsftpd.conf
